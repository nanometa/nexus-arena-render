# react-sky@1.1.0 declares peer react@^16 but the project uses react@18.2.
# This flag lets `npm install` resolve the (documented) peer conflict cleanly,
# so the standard install command works without passing --legacy-peer-deps manually.
legacy-peer-deps=true
